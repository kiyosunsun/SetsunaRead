{
  "identifier": "default",
  "description": "Default capabilities for SetsunaRead",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "fs:default",
    "fs:allow-read-file",
    "fs:allow-exists",
    "dialog:allow-open"
  ]
}
